
// alert("Hello World!");

let message; 
message = "Hello World!";

alert("message")

// define multiple variables
// Camel case: myVariableName and no number in front
let user = "brian",
    age = "28",
    message = "hello";

// Rule of thumb to set constants as All Cap
const WORD = "word";

// conversion is String(x) or Number(x)

if (year == 2015) {
    alert("Correct");
} 
else if (year > 2015) {
    alert("late");
}
else {
    alert("whoop");
}

// OR || and AND && NOT !

for (let i = 0; i < 3; i++) { // shows 0, then 1, then 2
    alert(i);
  }

switch(x) {
  case 'value1':  // if (x === 'value1')
    ...
    [break]

  case 'value2':  // if (x === 'value2')
    ...
    [break]

  default:
    ...
    [break]
}